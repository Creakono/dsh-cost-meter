/**
 * Browser-side pricing surface. The implementation lives in the shared
 * `src/pricing.ts` module so the host ledger and the settings/dock UI use
 * exactly the same window rules and arithmetic.
 */
export * from '../pricing.ts'
